{
  "name": "get-chatid-bot-cf-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --keep-vars",
    "dev": "wrangler dev --ip 0.0.0.0",
    "start": "wrangler dev --ip 0.0.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.3"
  }
}
